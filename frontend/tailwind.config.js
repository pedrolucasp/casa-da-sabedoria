// tailwind.config.js
// XXX: I think this won't work for the new tailwind?
module.exports = {
  theme: {
    extend: {
      fontFamily: {
        sans: ["Inter", "system-ui", "sans-serif"],
        mono: ["Menlo", "monospace"], // TODO: Replace with something else
      },
    },
  },
}

